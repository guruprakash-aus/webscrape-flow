import type { TaskParamType } from '@/types/task';

const COLOR_FOR_HANDLE: Record<TaskParamType, string> = {
  BROWSER_INSTANCE: '!bg-sky-400',
  STRING: '!bg-amber-400'
};

export default COLOR_FOR_HANDLE;
